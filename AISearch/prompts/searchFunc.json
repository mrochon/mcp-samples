{{
    "name": "call_search",
    "description": "Given a question, set additional Azure Search query parameters required to answer the question. ",
    "parameters": {{
        "type": "object",
        "properties": {{
            "orderBy": {{
                "type": "string",
                "description": "{sortDescription}"
            }},
            "filter": {{
                "type": "string",
                "description": "{filterDescription}"
            }},
            "search": {{
                "type": "string",
                "description": "{searchDescription}"
            }},
            "top": {{
                "type": "integer",
                "description": "The number of search results to return. The default is 50. The maximum is 1000. If less than 50 are needed to satisfy the query, set this field to the needed number."
            }}
        }}
    }}
}}